<script context="module">
	import { icons } from './icons';
</script>

<script>export let name = "placeholder";
export let x = "0px";
export let y = "0px";
export let viewBox = icons[name].viewBox || "0 0 512 512";
export let fill = "fill-current";
export let width = "w-5";
export let height = "h-5";
export let title = "";
const cBase = "inline-block outline-none";
$:
  classesBase = `${cBase} ${fill} ${width} ${height}`;
</script>

<!-- prettier-ignore -->
<svg
    xmlns="http://www.w3.org/2000/svg"
    {x} {y}
    {viewBox}
    class="svg-icon {classesBase} {$$props.class ?? ''}"
    focusable="false"
	data-testid="svg-icon"
>
    {#if title}<title>{title}</title>{/if}
    {#if $$slots.default}
        <slot />
    {:else}
        <path d={icons[name].path} />
    {/if}
</svg>
